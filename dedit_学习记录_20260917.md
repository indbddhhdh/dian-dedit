dedit 学习记录 — Level 0 / Level 1

日期： 2026-09-17

项目： C + ncurses 终端文本编辑器 dedit

本阶段成果： 完成 GitHub 仓库搭建与推送、Level 1.3（Ctrl+Q 退出）、Level 1.4（命令行打开并查看文本文件）

一、今天做了什么

在 GitHub 创建公开仓库 dian-dedit，常规方法不行，通过 SSH 方式把本地代码上传并打通推送。

完成 Level 1.3：把退出键从 Esc 改为 Ctrl+Q，退出后正确恢复终端。

完成 Level 1.4：程序通过命令行参数接收文件名，读取并显示内容，正确处理换行、空文件、文件打不开的情况，Ctrl+Q 可随时退出。

额外排查并在限定范围内解决了 UTF-8 中文显示乱码问题。

二、学到的知识点

1.Git 与 GitHub

git config 只对当前仓库生效，不影响全局。

git add 文件名 可以避免把测试文件、编译产物一起提交。

.gitignore 用来排除可执行文件等不该上传的东西。

2.终端与 ncurses

initscr() 进入 ncurses 全屏模式，endwin() 退出并把终端恢复成正常 shell；两者必须配对，且 endwin() 只能在初始化之后调用。

raw() 关闭行缓冲和特殊键拦截，使按键无需回车即可被读到，Ctrl+Q 这类控制键也不会被终端流控截走。

raw() 还会让终端进入 8-bit 干净透传：不对输入输出做特殊转换（如回车/换行映射），每个字节原样传递给程序。

printw 是 ncurses 版的 printf，内容先进虚拟屏幕，要 refresh() 才上屏。

getyx(stdscr, y, x) 读取当前光标位置；move(y, x) 移动光标。

程序退出时走到 endwin()，终端即恢复正常。

3.键盘编码

Ctrl + 字母得到的 ASCII 码 = 字母码按位与 0x1F（屏蔽第 6 位），因此大小写结果相同。

Ctrl+Q = 17，Ctrl+C = 3，Esc = 27。

Level 1.3 的实现就是把主循环退出判断条件从 27 改成 17。

4.命令行参数

入口写法：int main(int argc, char *argv[])；C 语言没有默认参数，如果没有声明就接收不到。

argc 是参数个数（至少为 1），argv[0] 是程序名，argv[1] 是第一个真正参数，argv[argc] 为 NULL。

运行方式：./dedit 文件名。

5.C 文件读取

fopen(path, "r") 失败返回 NULL，可用 perror 打印带系统原因的错误提示（不存在 / 无权限会自动区分）。

fread(ptr, 1, 1024, file) 按字节读取，返回实际读到的字节数，且会把读取到的赋值给数组；读到文件尾返回 0。

C 字符串依赖结尾的 \0，fread 不会自动补；用 %s 输出前必须手动在有效长度处写 \0，否则会出现乱码。

变量作用域：在 if {...} 内声明的指针，出了大括号就不可调用。

6.UTF-8 与中文显示

UTF-8 一个汉字占 3 个字节，但终端显示宽度是 2 列；ASCII 是 1 字节 1 列。

终端必须连续收到一个汉字的全部字节才能正确拼出该字；逐字节输出并在中间穿插光标移动 / refresh，会把字节序列切断，显示成乱码。

正确做法：setlocale(LC_ALL, "")（放在 initscr() 之前）+ 链接宽字符库 -lncursesw + 按行攒好后用一次 printw("%s", ...) 整行输出。

系统 locale 用 locale 命令查看，关键看 LANG 是否为 .UTF-8；LC_ALL 默认为空是正常的。

三、踩过的坑与解决

GitHub HTTPS 推送失败：DNS 异常把 github.com 解析到 127.0.0.1，IP 直连又有 TLS 证书校验问题 → 改用 SSH 密钥推送。

argc/argv undeclared：main 仍写成无参形式，C 没有默认参数 → 改成 int main(int argc, char *argv[])。

file undeclared：文件指针声明在 if 内层，循环在块外使用 → 统一放进同一作用域。

编译报括号不配对：fread(...) 少一个右括号，连锁导致后面花括号报错 → 补齐括号。

文件不存在时段错误：只 perror 没 return，继续拿 NULL 去 fread → endwin(); perror(...); return 1;。

按任意键都退出：getch() 只读了一次键 → 改成 while 循环，直到读到 Ctrl+Q（17）。

中文第二行乱码：换行时既 move 又打印了 \n，且逐字节输出切断汉字 → 去掉多余输出，改为整行输出。

第二行打印成第一行内容：行缓冲误用了 fread 块下标 i，第二行被写到 \0 之后 → 新增独立行下标，遇换行归零。

中文仍乱码：未设 locale 且链的是非宽字符库 → setlocale(LC_ALL,"") + -lncursesw。

四、独立完成与借助 AI 的真实边界

我自己完成 / 判断的：

Level 1.3 主动查清楚 Ctrl 组合键的编码规律，并自己把退出条件 27 改为 17。

Level 1.4 的整体功能代码（接收参数、fopen/fread 读取、循环显示、Ctrl+Q 循环等待）由我动手编写。

自己发现换行分支里多写了一次 printw('%c', '\n')（当时状态不好手滑）。

自己想到用字符串 + \n 表示文本、用 \0 处理字符串结尾等基础思路。

在 AI 提示 / 讲解后才明白或定位的：

程序初始化骨架（initscr / raw / keypad / noecho / set_escdelay 等）最早由 AI 提供，Level 1.1–1.3 属于"部分依赖 AI"。

raw() 的作用原理、为什么 Ctrl+Q 不会被终端拦截，是经 AI 讲解后理解的。

"按行计数处理换行"的思路，受 AI 一句"printw 不建议用 \n，用 move 更可控"启发，不是我独立想到。

段错误（NULL 继续 fread）、getch 只读一次键、行下标应与块下标分离等 bug，在 AI 引导下定位。

UTF-8 乱码的根因（多字节连续性、locale、宽字符库）及解决方案（setlocale + -lncursesw + 整行输出）由 AI 讲解，我负责理解、动手改和验证。

五、明天计划

回顾 Level 1 全部内容，保证能看到的点理解

开始 Level 2。
